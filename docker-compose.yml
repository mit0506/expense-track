
services:
  web:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - ./data:/app/instance
    environment:
      - FLASK_ENV=production
      - DATABASE_URL=sqlite:////app/instance/expenses.db
      - SECRET_KEY=your-production-secret-key-change-me
